# Dependencies
node_modules/

# Env / secrets — chứa WALLET_ADDRESS thật, không commit
.env
.env.local

# Data thật — chứa thông tin ví/giao dịch cá nhân, không commit
backend/data/*.xlsx
backend/data/*.xls
backend/data/*.csv

# Google service account credentials — chứa private key, tuyệt đối không commit
backend/data/*.json

# Build output
frontend/dist/

# Logs / OS junk
*.log
.DS_Store
