# Dependencies
node_modules/

# Env / secrets — contains real WALLET_ADDRESS, never commit
.env
.env.local

# Real data — contains personal wallet/transaction info, never commit
backend/data/*.xlsx
backend/data/*.xls
backend/data/*.csv

# Google service account credentials — contains a private key, never commit
backend/data/*.json

# Personal AI-assistant working notes, not project documentation
CLAUDE.md

# Build output
frontend/dist/

# Logs / OS junk
*.log
.DS_Store
